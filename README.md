# TheWave
Application web développée en Python intégrant une gestion de base de données relationnelle (PostgreSQL via psycopg2). Architecture MVC avec interface dynamique (HTML/CSS) et script d'automatisation SQL 
